# mac-activity-report

目的: 社員のMac操作ログ(アプリ・ウィンドウタイトル)を自動記録し、AIが日次で「具体的な作業内容・無駄・自動化候補」を要約してNotionにレポートする。
ユーザー: 経営者(福島)が閲覧。記録対象は社員(本人には事前に告知済み)。
寿命: 事業基盤(継続運用予定)。

## このプロジェクトの決めごと

- スタック: docs/decisions/0001-stack.md 参照
- 社員のMacにはAPIキーを一切置かない。記録(ActivityWatch)は完全ローカル・ネットワーク送信なし
- 集約はGoogle共有ドライブ経由。要約・Notion投稿は管理者のMacの`claude -p`(既存ログイン)だけで行う。新規APIキー発行なし
- 登場人物は実名のままレポートに残す(2026-08-28、社長の指示で匿名化しない方針に確定)
- ウィンドウタイトルのみで記録(スクリーンショットは無し)。精度が不足する場合のみ追加を検討

- 社員管理ダッシュボード(dashboard/、Cloudflare Workers): https://log.bonkers.llc （bonkers.llcドメインのサブドメイン、Workerの独自ドメイン割当）。マネージャーがログインし、①設定(共有ドライブパス・NotionDB URL・集計取り込みトークン) ②社員の追加(社員ごとの格納先パス・NotionページURL・セットアップコマンド発行) ③集計(社員別/社内全体のアプリ別稼働時間) ④マネージャーの招待、ができる。認証は自前実装(docs/decisions/0002-dashboard-auth.md参照)
- アプリ別の稼働時間はD1の`activity`テーブルに集計され、report/ingest-activity.mjsが日次で送信する(AI不使用、決定的な集計のみ)
- 時間帯ごとのタイムラインは別DBに分けず、「社員稼働レポート」の各ページ本文に表として入れる(2026-08-29、社長の指示。1人の1日を1ページで完結させ、行クリックで中身が見られる形にする)

## 合格基準

- dev-brain標準(typecheck/test/実画面確認/4点報告)に加えて:
- 実際の1日分のログから、具体的な作業内容がNotionレポートとして正しく読み取れること
- 社員のMac側に一切のAPIキー・認証情報が存在しないこと
